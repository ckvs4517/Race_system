name: Inline quick score patch

on:
  push:
    branches:
      - feat/inline-quick-score
    paths:
      - .github/workflows/inline-quick-score-patch.yml

permissions:
  contents: write

jobs:
  patch-test-commit:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout feature branch
        uses: actions/checkout@v5
        with:
          ref: feat/inline-quick-score
      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22
      - name: Install dependencies
        run: npm ci
      - name: Implement inline quick score
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import re

          def replace_once(path, old, new, label):
              p = Path(path)
              text = p.read_text(encoding='utf-8')
              if old not in text:
                  raise RuntimeError(f'expected block not found: {label}')
              p.write_text(text.replace(old, new, 1), encoding='utf-8')

          def regex_once(path, pattern, replacement, label):
              p = Path(path)
              text = p.read_text(encoding='utf-8')
              text, count = re.subn(pattern, replacement, text, count=1, flags=re.S)
              if count != 1:
                  raise RuntimeError(f'expected regex block not found: {label} ({count})')
              p.write_text(text, encoding='utf-8')

          replace_once(
              'src/core/quick-score.js',
              "function parseNonNegativeInteger(value) {",
              "export function parseQuickScoreText(value) {\n  const text = String(value ?? '').trim()\n    .replaceAll('：', ':')\n    .replace(/[－–—]/g, '-');\n  let match = text.match(/^(\\d+)\\s*[:\\/-]\\s*(\\d+)$/) || text.match(/^(\\d+)\\s+(\\d+)$/);\n  if (!match && /^\\d{2}$/.test(text)) match = [text, text[0], text[1]];\n  if (!match) throw new Error('請輸入例如 42、4:2、4 2 或 4-2 的最終比分。');\n  return validateQuickScoreInput(match[1], match[2]);\n}\n\nfunction parseNonNegativeInteger(value) {",
              'quick score text parser',
          )

          replace_once(
              'src/main.js',
              "import { readQuickScoreMode, validateQuickScoreInput, writeQuickScoreMode } from './core/quick-score.js';",
              "import { parseQuickScoreText, readQuickScoreMode, writeQuickScoreMode } from './core/quick-score.js';",
              'quick score import',
          )

          main_block = r'''function updateQuickScoreInlineSubmitLabel(form) {
  const submit = form.querySelector('[data-quick-score-submit]');
  const input = form.querySelector('[name="score"]');
  if (!submit) return;
  const prefix = quickScoreDraft?.error ? '重新送出' : '確認登分';
  try {
    const score = parseQuickScoreText(input?.value || '');
    submit.textContent = `${prefix} ${score.scoreA} : ${score.scoreB}`;
  } catch {
    submit.textContent = prefix;
  }
}

function quickScoreInlineMarkup() {
  return `<form class="quick-score-inline-form" data-quick-score-form novalidate>
    <div class="quick-score-inline-heading"><span data-quick-score-context>快速登分</span><button type="button" data-quick-score-close>取消</button></div>
    <div class="quick-score-inline-entry"><label><span class="sr-only">最終比分</span><input type="text" inputmode="numeric" autocomplete="off" enterkeyhint="done" name="score" placeholder="例如 42" aria-label="最終比分" required></label><button type="submit" class="button button-primary" data-quick-score-submit>確認登分</button></div>
    <p class="quick-score-help">可輸入 42、4:2、4 2 或 4-2；任一方達到或超過 4 分即結束，敗方必須低於 4 分。</p>
    <p class="quick-score-error" data-quick-score-error role="alert" hidden></p>
  </form>`;
}

function removeQuickScoreInline() {
  app.querySelector('[data-quick-score-inline]')?.remove();
  app.querySelectorAll('.match-card.is-quick-scoring').forEach((card) => card.classList.remove('is-quick-scoring'));
}

function hydrateQuickScoreInline(inline) {
  if (!quickScoreDraft) return false;
  const { tournament, round, match } = quickScoreMatch(quickScoreDraft);
  if (!tournament || !round || !match || tournament.status !== '進行中' || match.status !== '可開始') return false;
  const form = inline.querySelector('[data-quick-score-form]');
  form.querySelector('[data-quick-score-context]').textContent = `${quickScoreDraft.arenaLabel || '戰鬥台 1'} · ${round.name} · ${match.playerA} vs ${match.playerB}`;
  const input = form.querySelector('[name="score"]');
  input.value = quickScoreDraft.scoreText ?? '';
  const errorNode = form.querySelector('[data-quick-score-error]');
  errorNode.textContent = quickScoreDraft.error || '';
  errorNode.hidden = !quickScoreDraft.error;
  const submit = form.querySelector('[data-quick-score-submit]');
  submit.disabled = Boolean(quickScoreDraft.submitting);
  if (quickScoreDraft.submitting) submit.textContent = '正在同步…';
  else updateQuickScoreInlineSubmitLabel(form);
  return true;
}

function focusQuickScoreInline(inline) {
  if (!quickScoreDraft || quickScoreDraft.submitting) return;
  const input = inline.querySelector('[name="score"]');
  input?.focus();
  input?.select?.();
}

function bindQuickScoreInlineForm(state, inline) {
  const form = inline.querySelector('[data-quick-score-form]');
  const input = form.querySelector('[name="score"]');
  const errorNode = form.querySelector('[data-quick-score-error]');

  const syncDraft = () => {
    if (!quickScoreDraft) return;
    quickScoreDraft.scoreText = input.value;
    quickScoreDraft.error = '';
    errorNode.textContent = '';
    errorNode.hidden = true;
    updateQuickScoreInlineSubmitLabel(form);
  };
  input.addEventListener('input', syncDraft);
  form.querySelector('[data-quick-score-close]')?.addEventListener('click', () => {
    quickScoreDraft = null;
    removeQuickScoreInline();
  });

  form.addEventListener('submit', async (event) => {
    event.preventDefault();
    if (!quickScoreDraft || quickScoreDraft.submitting) return;
    syncDraft();
    let score;
    try {
      score = parseQuickScoreText(input.value);
    } catch (error) {
      quickScoreDraft.error = error.message;
      errorNode.textContent = error.message;
      errorNode.hidden = false;
      updateQuickScoreInlineSubmitLabel(form);
      return;
    }

    const { tournament, match } = quickScoreMatch(quickScoreDraft);
    if (!tournament || match?.status !== '可開始') {
      quickScoreDraft.error = '這場對局已被更新，請確認最新賽程。';
      errorNode.textContent = quickScoreDraft.error;
      errorNode.hidden = false;
      return;
    }

    const draft = { ...quickScoreDraft };
    const scrollPosition = { top: window.scrollY, left: window.scrollX };
    quickScoreDraft.submitting = true;
    quickScoreDraft.error = '';
    hydrateQuickScoreInline(inline);
    try {
      await executeTournamentAction(tournament.id, 'record_match', {
        roundIndex: draft.roundIndex,
        matchIndex: draft.matchIndex,
        scoreA: score.scoreA,
        scoreB: score.scoreB,
      }, { retryOnConflict: false });
      quickScoreDraft = null;
      scheduleScrollRestore = scrollPosition;
      render();
      showToast(`已登錄 ${match.playerA} ${score.scoreA}：${score.scoreB} ${match.playerB}`);
    } catch (error) {
      const latest = getState();
      const latestMatch = latest.tournaments.find((item) => item.id === draft.tournamentId)
        ?.rounds?.[draft.roundIndex]?.matches?.[draft.matchIndex];
      if (latestMatch?.status !== '可開始') {
        quickScoreDraft = null;
        scheduleScrollRestore = scrollPosition;
        render();
        showToast('這場對局已由其他裝置完成或變更，已載入最新賽果。', 'error');
        return;
      }
      quickScoreDraft = { ...draft, error: error.message || '同步失敗，請確認網路後重新送出。', submitting: false };
      scheduleScrollRestore = scrollPosition;
      render();
    }
  });
}

function mountQuickScoreInline(state, focus = true) {
  removeQuickScoreInline();
  if (!state.isAdmin || !readQuickScoreMode() || !quickScoreDraft || quickScoreDraft.tournamentId !== state.selectedTournamentId) return;
  const { tournament, match } = quickScoreMatch(quickScoreDraft, state);
  if (!tournament || tournament.status !== '進行中' || match?.status !== '可開始') {
    quickScoreDraft = null;
    return;
  }
  const card = app.querySelector(`.match-card.is-ready[data-round-index="${quickScoreDraft.roundIndex}"][data-match-index="${quickScoreDraft.matchIndex}"]`);
  if (!card) {
    quickScoreDraft = null;
    return;
  }
  card.classList.add('is-quick-scoring');
  const inline = document.createElement('div');
  inline.className = 'quick-score-inline';
  inline.dataset.quickScoreInline = '';
  inline.innerHTML = quickScoreInlineMarkup();
  card.insertAdjacentElement('afterend', inline);
  if (!hydrateQuickScoreInline(inline)) {
    quickScoreDraft = null;
    removeQuickScoreInline();
    return;
  }
  bindQuickScoreInlineForm(state, inline);
  if (focus) queueMicrotask(() => focusQuickScoreInline(inline));
}

function openQuickScoreInline(state, card) {
  const tournament = state.tournaments.find((item) => item.id === state.selectedTournamentId);
  const roundIndex = Number(card.dataset.roundIndex);
  const matchIndex = Number(card.dataset.matchIndex);
  const match = tournament?.rounds?.[roundIndex]?.matches?.[matchIndex];
  if (!state.isAdmin || !tournament || tournament.status !== '進行中' || match?.status !== '可開始') return;
  const sameMatch = quickScoreDraft?.tournamentId === tournament.id
    && quickScoreDraft?.roundIndex === roundIndex
    && quickScoreDraft?.matchIndex === matchIndex;
  if (sameMatch) {
    const inline = app.querySelector('[data-quick-score-inline]');
    if (inline) focusQuickScoreInline(inline);
    return;
  }
  const arenaLabel = card.closest('.battle-station')?.querySelector('.battle-station-title span')?.textContent?.trim() || '戰鬥台 1';
  quickScoreDraft = {
    tournamentId: tournament.id,
    roundIndex,
    matchIndex,
    arenaLabel,
    scoreText: '',
    error: '',
    submitting: false,
  };
  mountQuickScoreInline(state);
}

function bindScheduleEvents(state) {'''

          regex_once(
              'src/main.js',
              r'function updateQuickScoreSubmitLabel\(dialog\) \{.*?\nfunction bindScheduleEvents\(state\) \{',
              main_block,
              'replace modal quick score helpers with inline helpers',
          )
          replace_once('src/main.js', '  bindQuickScoreDialog(state);', '  mountQuickScoreInline(state);', 'bind inline quick score')
          replace_once('src/main.js', '      openQuickScoreDialog(state, card);', '      openQuickScoreInline(state, card);', 'open inline quick score')

          replace_once(
              'src/views/schedule.js',
              "  const quickScoreDialog = canManage && tournament.status === '進行中' ? quickScoreDialogView() : '';\n",
              '',
              'remove quick score dialog variable',
          )
          replace_once(
              'src/views/schedule.js',
              '${workflowPanel}${eventInfoPanel}${champion}${registrationPanel}${participantPanel}<div class="bracket-guide">${guide}</div>${quickScoreNotice}${pairingPanel}${swissDecision}${roundRobinDecision}${bracket}${leaderboard}${quickScoreDialog}</section>`;',
              '${workflowPanel}${eventInfoPanel}${champion}${registrationPanel}${participantPanel}<div class="bracket-guide">${guide}</div>${quickScoreNotice}${pairingPanel}${swissDecision}${roundRobinDecision}${bracket}${leaderboard}</section>`;',
              'remove dialog from schedule output',
          )
          regex_once(
              'src/views/schedule.js',
              r'\nfunction quickScoreDialogView\(\) \{.*?\n\}\n\nfunction currentRoundEntries',
              '\nfunction currentRoundEntries',
              'remove obsolete dialog view',
          )

          replace_once(
              'index.html',
              '  <link rel="stylesheet" href="src/styles/schedule-responsive.css" />\n',
              '  <link rel="stylesheet" href="src/styles/schedule-responsive.css" />\n  <link rel="stylesheet" href="src/styles/quick-score-inline.css" />\n',
              'load inline quick score styles',
          )

          Path('src/styles/quick-score-inline.css').write_text(r'''/* 主控快速登分：非阻塞式卡片內展開，手機與平板可持續查看其他對局。 */
.match-card.is-quick-scoring {
  border-color: #f2c94c;
  box-shadow: 0 0 0 1px #f2c94c28 inset, 0 10px 24px #0005;
}
.quick-score-inline {
  margin-top: -3px;
  padding: 12px;
  background: linear-gradient(145deg, #151b24, #0d1219);
  border: 1px solid #f2c94c88;
  border-radius: 9px;
  box-shadow: 0 12px 28px #0006;
}
.quick-score-inline-form { display: grid; gap: 9px; }
.quick-score-inline-heading { min-width: 0; display: flex; align-items: center; justify-content: space-between; gap: 10px; }
.quick-score-inline-heading span { min-width: 0; overflow: hidden; color: #ffe785; font-size: 11px; font-weight: 900; line-height: 1.4; text-overflow: ellipsis; white-space: nowrap; }
.quick-score-inline-heading button { flex: 0 0 auto; padding: 5px 8px; background: transparent; color: #aeb7c5; border: 0; border-radius: 6px; font-size: 11px; font-weight: 800; cursor: pointer; }
.quick-score-inline-heading button:hover { background: #ffffff0a; color: #fff; }
.quick-score-inline-entry { display: grid; grid-template-columns: minmax(0, 1fr) auto; align-items: stretch; gap: 8px; }
.quick-score-inline-entry label { min-width: 0; }
.quick-score-inline-entry input { width: 100%; height: 52px; padding: 0 14px; background: #070a0f; color: #fff; border: 1px solid #566277; border-radius: 8px; font: 900 28px/1 var(--font-display); letter-spacing: 1px; text-align: center; font-variant-numeric: tabular-nums; }
.quick-score-inline-entry input:focus { border-color: #f2c94c; box-shadow: 0 0 0 3px #f2c94c18; outline: 0; }
.quick-score-inline-entry .button { min-height: 52px; padding-inline: 16px; white-space: nowrap; }
.quick-score-inline .quick-score-help { margin: 0; font-size: 10px; line-height: 1.45; }
.quick-score-inline .quick-score-error { margin: 0; padding: 8px 10px; font-size: 11px; }

@media (max-width: 620px) {
  .quick-score-inline { padding: 10px; }
  .quick-score-inline-entry { grid-template-columns: minmax(0, 1fr) 112px; gap: 7px; }
  .quick-score-inline-entry input { height: 50px; font-size: 27px; }
  .quick-score-inline-entry .button { min-height: 50px; padding-inline: 10px; font-size: 12px; }
  .quick-score-inline-heading span { font-size: 10px; }
  .quick-score-inline .quick-score-help { font-size: 9px; }
}

@media (max-width: 390px) {
  .quick-score-inline-entry { grid-template-columns: minmax(0, 1fr) 98px; }
  .quick-score-inline-entry .button { font-size: 11px; }
}
''', encoding='utf-8')

          replace_once(
              'tests/quick-score.test.mjs',
              "import { QUICK_SCORE_MODE_KEY, readQuickScoreMode, validateQuickScoreInput, writeQuickScoreMode } from '../src/core/quick-score.js';",
              "import { QUICK_SCORE_MODE_KEY, parseQuickScoreText, readQuickScoreMode, validateQuickScoreInput, writeQuickScoreMode } from '../src/core/quick-score.js';",
              'quick score parser test import',
          )
          replace_once(
              'tests/quick-score.test.mjs',
              "expect(JSON.stringify(validateQuickScoreInput('6', '3')) === JSON.stringify({ scoreA: 6, scoreB: 3 }), '勝方超過 4 分但敗方未達 4 分可快速登分');\n",
              "expect(JSON.stringify(validateQuickScoreInput('6', '3')) === JSON.stringify({ scoreA: 6, scoreB: 3 }), '勝方超過 4 分但敗方未達 4 分可快速登分');\nfor (const compact of ['42', '4:2', '4：2', '4 2', '4-2']) expect(JSON.stringify(parseQuickScoreText(compact)) === JSON.stringify({ scoreA: 4, scoreB: 2 }), `${compact} 可解析為 4:2`);\nlet invalidCompactRejected = false;\ntry { parseQuickScoreText('64'); } catch { invalidCompactRejected = true; }\nexpect(invalidCompactRejected, '單欄快速比分仍套用先到 4 分規則');\n",
              'single field parser assertions',
          )
          replace_once(
              'tests/quick-score.test.mjs',
              "expect(quick.includes('data-quick-score-dialog') && quick.includes('data-quick-score-form'), '賽程頁內建快速比分小視窗');\nexpect(!publicView.includes('data-action=\"toggle-quick-score\"') && !publicView.includes('data-quick-score-dialog'), '公開賽程不提供快速登分控制');",
              "expect(!quick.includes('data-quick-score-dialog'), '快速登分不再預載阻塞式 modal');\nexpect(!publicView.includes('data-action=\"toggle-quick-score\"'), '公開賽程不提供快速登分控制');",
              'modal regression assertions',
          )

          full_flow = Path('tests/full-flow.test.js')
          text = full_flow.read_text(encoding='utf-8')
          pattern = re.compile(r"  click\('\.match-card\.is-ready'\);\n  await waitFor\('\[data-quick-score-dialog\]\[open\]'\);.*?  expect\(\[\.\.\.records\.values\(\)\]\.some\(\(item\) => item\.rounds\?\.some\(\(round\) => round\.matches\.some\(\(match\) => match\.status === '已完成' && match\.scoreA === 6 && match\.scoreB === 3\)\)\), '快速登分可送出勝方超過 4 分且敗方低於 4 分的結果'\);", re.S)
          replacement = r'''  click('.match-card.is-ready');
  await waitFor('[data-quick-score-inline]');
  expect(!document.querySelector('[data-scoreboard].match-mode'), '快速登分點對局不會跳到完整記分板');
  expect(!document.querySelector('[data-quick-score-dialog][open]'), '快速登分不使用阻塞式 modal');
  const firstQuickCard = document.querySelector('.match-card.is-quick-scoring');
  const otherReadyMatch = [...document.querySelectorAll('.match-card.is-ready')].find((node) => node !== firstQuickCard);
  expect(otherReadyMatch, '快速登分展開時其他待打對局仍留在畫面上');
  otherReadyMatch.click();
  await waitUntil(() => document.querySelector('.match-card.is-quick-scoring') === otherReadyMatch);
  expect(document.querySelector('[data-quick-score-inline]'), '點另一場可直接把快速輸入區移到該對局');
  fill('[data-quick-score-form] [name="score"]', '63');
  failNextRecordMatch = true;
  submit('[data-quick-score-form]');
  await waitFor('[data-quick-score-inline] [data-quick-score-error]:not([hidden])');
  expect(document.querySelector('[data-quick-score-form] [name="score"]').value === '63', '快速登分同步失敗會保留尚未送出的單欄比分');
  submit('[data-quick-score-form]');
  await waitFor('.match-card.is-ready');
  expect(document.querySelector('[data-action="toggle-quick-score"]')?.getAttribute('aria-pressed') === 'true', '成功登分後仍維持快速登分模式');
  expect([...records.values()].some((item) => item.rounds?.some((round) => round.matches.some((match) => match.status === '已完成' && match.scoreA === 6 && match.scoreB === 3))), '單欄快速登分可送出 6:3 合法比分');'''
          text, count = pattern.subn(replacement, text, count=1)
          if count != 1:
              raise RuntimeError(f'full flow quick score block not found ({count})')
          full_flow.write_text(text, encoding='utf-8')
          PY
      - name: Project health
        run: node scripts/project-health.mjs
      - name: Focused quick score regression
        run: node tests/quick-score.test.mjs
      - name: Fast regression
        run: node scripts/test-fast.mjs
      - name: Full regression and browser flows
        run: node scripts/test-full.mjs --browser=required
      - name: Commit tested implementation
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html src/core/quick-score.js src/main.js src/views/schedule.js src/styles/quick-score-inline.css tests/quick-score.test.mjs tests/full-flow.test.js
          git diff --cached --check
          git commit -m "Use inline quick score entry"
          git push origin HEAD:feat/inline-quick-score
